module.exports = {
  plugins: {
    '@tailwindcss/postcss': {}, // Updated from 'tailwindcss'
    autoprefixer: {}, // Optional: Autoprefixer is often included with Tailwind, but explicit inclusion is fine.
  },
};